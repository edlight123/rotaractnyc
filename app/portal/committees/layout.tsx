import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Committees — Rotaract Portal' };

export default function CommitteesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
