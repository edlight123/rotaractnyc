import { Metadata } from 'next';

export const metadata: Metadata = { title: 'Reports & Exports — Rotaract Portal' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
