import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Admin Reminders — Rotaract Portal' };

export default function AdminRemindersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
