import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Events — Rotaract Portal' };

export default function EventsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
