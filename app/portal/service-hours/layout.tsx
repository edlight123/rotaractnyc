import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Service Hours — Rotaract Portal' };

export default function ServiceHoursLayout({ children }: { children: React.ReactNode }) {
  return children;
}
