rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Uploads/downloads are performed server-side via Firebase Admin SDK.
    // Default posture: deny all direct client access.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
